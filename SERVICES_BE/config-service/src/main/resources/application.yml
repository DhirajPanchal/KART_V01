spring:
  application:
    name: "config-service"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "classpath:/config"
#          search-locations: "file:///C://config"

server:
  port: 8401
